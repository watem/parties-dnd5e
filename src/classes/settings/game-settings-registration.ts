/*
import {ModuleName} from "../../constants";


export default class GameSettingsRegistration{
    static Register(){

    /*
    // TODO: select clock to use

    game.settings.register(ModuleName, "clock", {
        name: "Clock",
        hint: "The clock to use.",
        scope: "world",
        config: true,
        requiresReload: true,
        type: String,
        choices: clocks,
        default: "base",
        onChange: value => {},
    });

    // TODO set up submenu for interrupt times
    game.settings.register(ModuleName, "interrupts_midnight", {
        name: "Interrupt at midnight",
        hint: "Interrupt the party progression at midnight.",
        scope: "world",
        config: true,
        requiresReload: false,
        type: Boolean,
        default: false,
        onChange: value => {},
    });

    game.settings.register(ModuleName, "interrupts_dawn", {
        name: "Interrupt at dawn",
        hint: "Interrupt the party progression at dawn.",
        scope: "world",
        config: true,
        requiresReload: false,
        type: Boolean,
        default: false,
        onChange: value => {},
    });

    game.settings.register(ModuleName, "interrupts_midday", {
        name: "Interrupt at midday",
        hint: "Interrupt the party progression at midday.",
        scope: "world",
        config: true,
        requiresReload: false,
        type: Boolean,
        default: false,
        onChange: value => {},
    });

    game.settings.register(ModuleName, "interrupts_dusk", {
        name: "Interrupt at dusk",
        hint: "Interrupt the party progression at dusk.",
        scope: "world",
        config: true,
        requiresReload: false,
        type: Boolean,
        default: false,
        onChange: value => {},
    });

    // TODO: food/water reminders
    // TODO: reminder after 8 hours travelling and every hour of travel afterwards

    */ /*
    }

    static RegisterStorage() {

    }


    static InterruptMenu() {

    }

    static BaseClockMenu() {
    /*
    hours/day
    minutes/hour
    seconds/minute

    dawn_time, dusk_time
    */ /*
    }
}
*/